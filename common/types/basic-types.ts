export type TKeyValue = string;

export enum DoubleTypes {
    Exact = "Exact",
    Catalog = "Catalog",
    Skintone = "Skintone",
    Cas = "Cas",
    Slider = "Slider",
}

export type TTicketId = number;
